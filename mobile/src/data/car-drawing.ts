/**
 * Re-export of the generated drawing. tsconfig maps `@/assets/*` to the
 * repo-level assets folder, so the generated module under src/assets is
 * reached through this relative path instead.
 */
export * from '../assets/car/drawing';
